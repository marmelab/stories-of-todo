import { Canvas, Meta } from '@storybook/blocks';

import * as ListStories from './List.stories';

<Meta title="List" of={ListStories}/>

# List

The `List` component is a simple list of todos. It fetches the list of todos with react-query.
It is desinged to be used inside the `<Todos>` component.

## Usage

```jsx
import { List } from './List';

export const Default = () => <List />;
```

<Canvas of={ListStories.Default} />